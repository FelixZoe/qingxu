# 对外监听地址。前面还有 1Panel/OpenResty 时保持回环监听。
QINGXU_LISTEN=127.0.0.1:8090
QINGXU_VERSION=local

# 必须替换成 `openssl rand -hex 32` 生成的 64 个十六进制字符。
SYNC_TOKEN=replace-with-64-hex-characters
SYNC_CORS_ORIGINS=https://todo.darker.one
SYNC_MAX_BODY_BYTES=2097152
GOMEMLIMIT=48MiB
